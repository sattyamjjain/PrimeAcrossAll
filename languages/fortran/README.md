# Fortran Prime Checker

This Fortran program checks whether a given number is prime.

## How to Run

1. Install a Fortran compiler (e.g., `gfortran`).
2. Compile the program:
    `gfortran -o prime languages/fortran/prime.f90`
3. Run the program:
    `./prime`