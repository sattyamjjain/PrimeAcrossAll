# OCaml Prime Number Checker

This is an OCaml implementation to check if a number is prime.

## Usage

1. Install OCaml: [https://ocaml.org](https://ocaml.org)
2. Compile and run the code:

```bash
ocamlc -o prime prime.ml
./prime <number>
