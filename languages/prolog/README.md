# Prolog Prime Number Checker

This Prolog program checks if a given number is prime.

## How to run

To run the program, use the following command:

```bash
swipl -q -t main languages/prolog/prime.pl
