# Racket Prime Number Checker

This implementation checks if a number is prime using recursion in Racket.

## Usage

To check if a number is prime, run the script using Racket:

```bash
racket prime.rkt
